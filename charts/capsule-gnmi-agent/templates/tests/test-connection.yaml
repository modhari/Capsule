apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "capsule-gnmi-agent.fullname" . }}-test-connection"
  labels:
    {{- include "capsule-gnmi-agent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "capsule-gnmi-agent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
